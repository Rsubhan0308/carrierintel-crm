{
  "name": "us-truck-carrier-scraper",
  "version": "1.0.0",
  "description": "High-Speed FMCSA, SAFER & CarrierSource Scraper with Sales CRM for Truck Dispatch Companies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "postinstall": "pip install -r requirements.txt || pip3 install -r requirements.txt || python3 -m pip install -r requirements.txt || true"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.22.2"
  }
}
